all:
  children:
    production:
      hosts:
        app-server.example.com:
          ansible_host: 192.0.2.10
          ansible_user: deploy
    staging:
      hosts:
        staging-app.example.com:
          ansible_host: 192.0.2.20
          ansible_user: deploy
