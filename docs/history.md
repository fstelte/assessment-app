# Change History

This log tracks major decisions and milestones as the scaffold application evolves.

| Date | Change | Notes |
| --- | --- | --- |
| 2026-04-10 | SSP boundary UX, controls picker overhaul, and risk controls sync (2026.4.6) | Three focused improvements: **(1) SSP authorization boundary UX** — The SSP edit page now surfaces a collapsible BIA-sourced boundary panel that lists the linked context's component names and interconnection system names as chip badges (blue for components, green for interconnections). A "Fill from BIA" button copies these items into the `authorization_boundary` textarea; a manual add-item input lets editors append individual entries without touching the textarea directly. When `authorization_boundary` is empty on a GET request the field is auto-populated from the BIA items. A `render_field_with_help` macro is now used throughout the edit form, and `description` (i18n) help text was added to all SSP, interconnection, control-entry, and POA&M form fields. ~39 new translation keys in `en.json` and `nl.json`. **(2) Controls picker redesign** — The flat `<select multiple>` for CSA controls on the threat scenario form and the risk edit form has been replaced with a searchable scrollable checkbox panel. A live filter input narrows items by label text; a count badge shows how many controls are selected; CSS `:has(input:checked)` highlights selected rows; unselecting all via the treatment toggle clears the checkboxes and resets the filter. **(3) Risk controls auto-sync** — The risk `edit` route now re-syncs the linked risk's controls list from the threat scenario on every load (matching the existing components re-sync introduced in 2026.4.5), keeping controls in lock-step with the threat model without manual intervention. Minor Docker restore Dockerfile fix included. |
| 2026-04-09 | Field-level encryption, SSP/POA&M deep integration, and risk dashboard refactor (2026.4.5) | Three main areas of work: **(1) Field-level encryption** — New `scaffold/core/encryption.py` introduces `EncryptedString`, `EncryptedJSON`, and `EncryptedBinary` SQLAlchemy `TypeDecorator` types backed by `cryptography.Fernet` with key-rotation support via a comma-separated `FIELD_ENCRYPTION_KEYS` env var. `MFASetting.secret` and `MFASetting.backup_codes` are now stored encrypted at rest; existing plaintext values are handled transparently on read (graceful `InvalidToken` fallback). Migration `20260408_0001_encrypt_sensitive_mfa_fields` converts the column types. A new `encrypt-existing-secrets` CLI command backfills pre-existing plaintext records idempotently. `FIELD_ENCRYPTION_KEYS` documented in `env.production.example`. Test suite added (`test_field_encryption.py`). **(2) SSP/POA&M ↔ Threat deep integration** — New `sync_scenario_controls_to_ssp()` service in `ssp/services.py` propagates CSA controls from a `ThreatScenario` into the linked SSP's minimum security controls on scenario create/edit. New `sync_mitigation_to_poam()` service automatically creates or updates a `POAMItem` in the linked SSP whenever a `ThreatMitigationAction` is created or edited; `POAMItem` gains a `source_threat_mitigation_id` FK (migration `20260409_0001_add_source_threat_mitigation_id_to_poam`). New `/ssp/<id>/controls/sync-from-threats` POST endpoint for manual bulk-sync of threat controls. `seed_controls()` now also walks direct and DPIA-linked threat models to seed controls from their scenarios. **(3) Risk dashboard & sync improvements** — `sync_from_threats` now also updates already-linked scenarios (not just creates new ones). `_components_for_scenario()` now first tries to match the scenario's asset name to a BIA component by name instead of falling back to the whole scope immediately. `configure_risk_form()` and `component_choices()` accept an optional `context_scope_id` to scope the component dropdown to the relevant system. Risk edit route re-syncs components from the current threat model on every load. Risk dashboard CSS refactored: duplicate nested class definitions removed, Bootstrap CSS variables replaced with app theme variables (`--color-muted`, `--color-border`), hero grid rewritten with CSS Grid, and `.risk-actions` / `.risk-card-grid` layout helpers extracted. |
| 2026-04-07 | Threat → Risk workspace synchronisation (2026.4.4 / 2026.4.4a) | Added a direct link between `ThreatScenario` and the Risk workspace: new `risk_id` FK on `ThreatScenario` (migration `20260407_0001_add_risk_id_to_threat_scenarios`) and a `sync_scenario_to_risk()` service in `threat/services.py`. Scenarios with `treatment=mitigate` and an active status are **automatically** synced to a Risk item on create/edit; if treatment changes to accept/transfer/avoid, or the scenario reaches a terminal status (mitigated/accepted/closed), the linked risk is automatically closed. Added a `/risk/sync-from-threats` POST endpoint for bulk-syncing all eligible unlinked scenarios (button on Risk dashboard for admins and assessment managers). Added a per-scenario `/threat/<id>/scenarios/<id>/sync-risk` endpoint and a "Sync now" button on the scenario detail page, which also shows a "Risk workspace" field with a linked badge and a link to the risk edit page. The risk `edit` route now auto-refreshes chance/impact/components from the linked scenario on load; a new `_resolve_components_for_edit()` helper allows saving risks that include components introduced via the threat-sync. New CLI command `backfill-threat-risks` (with `--dry-run`) for migrating pre-existing scenarios. 10 new translation keys in `en.json` and `nl.json`. Hotfix 2026.4.4a resolved an authorization regression in risk and threat routes introduced by the same release. |
| 2026-04-04 | SSP module enhancements and threat model BIA linkage (2026.4.3) | Added `context_scope_id` FK to `ThreatModel` so threat models can be directly linked to a BIA `ContextScope`. Added a `_collect_threat_models` helper in SSP routes that aggregates related threat models via three paths: direct FK, via linked DPIA assessments, and an asset-name heuristic for pre-migration models. The SSP detail view (`view.html`) and print template (`print.html`) were substantially expanded to surface the linked threat models. SSP forms, controls, and interconnection templates restyled to match application Tailwind conventions. Translations expanded (233 new keys in both `en.json` and `nl.json`). |
| 2026-04-03 | System Security Plan (SSP) module (2026.4.2) | New **SSP** module conforming to NIST SP 800-18 Rev 1. One `SSPlan` per `ContextScope` (BIA system). Models: `SSPlan`, `SSPInterconnection`, `SSPControlEntry`, `SSPPoamItem`. FIPS 199 C/I/A categorisation auto-derived from BIA Consequences with manual override fields. System environment auto-populated from BIA component dependency fields. Security controls linked via Threat scenario and Risk register. System interconnections auto-populated from `ContextScope.interfaces` and individually editable. Plan of Action & Milestones (POA&M) table with item CRUD. PDF export via existing Playwright/WeasyPrint pipeline. Six Alembic migrations added. Full integration test suite (`test_ssp_routes.py`, 304 lines). Module registered as a navigation default. |
| 2026-04-03 | Dependency security fixes (2026.4.1 / 2026.4.2a) | Pinned updated dependencies to address known vulnerabilities. Removed `libncursesw6` from the production Dockerfile (post-Playwright install) to eliminate a vulnerable system library; applied the same hardening to the backup Dockerfile. Updated `poetry.lock` to pull in patched transitive dependencies. |
| 2026-03-28 | Redis integration, rate limiting, and background task scheduling (2026.3.16) | Introduced optional Redis-backed session storage (replaces filesystem sessions in production) and a `Flask-Caching` layer (`scaffold/core/cache.py`). Added `APScheduler` (via `Flask-APScheduler`) for periodic background tasks: BIA report pre-generation and Threat model enrichment tasks (`bia/tasks.py`, `threat/tasks.py`). Integrated `Flask-Limiter` with Redis as the storage backend; added per-route rate limits on auth and API endpoints with a styled `429` error page. New `REDIS_URL`, `RATELIMIT_*`, and scheduler toggle env vars documented in `env.production.example`. Rate-limiting test suite added (`test_rate_limiting.py`). |
| 2026-03-28 | Audit trail expansion and export audit logging (2026.3.16) | Massively expanded `_DEFAULT_AUTO_AUDIT_CONFIG` in `scaffold/core/audit.py` to cover all application models by default — no manual `AUDIT_LOG_MODEL_EVENTS` configuration required. Added identity sub-models (AADGroupMapping, MFASetting excluding sensitive credentials, PasskeyCredential excluding raw key material), expanded User fields (`username`, `azure_oid`, `aad_upn`), and added full coverage for BIA (BiaTier, AuthenticationMethod, InformationLabel, ContextScope, Component, ComponentEnvironment, Consequences, and more), CSA, DPIA, Risk, Incident, Maturity, and Threat models. Added `log_event` audit calls to every export endpoint across all modules (BIA: HTML/CSV/SQL/data-inventory/auth-overview/consequences/availability/dependencies; CSA: HTML/PDF; Incident: HTML/PDF; Maturity: HTML/PDF; Threat: CSV/HTML/PDF). Fixed nginx upstream resolution: replaced the static `upstream app_backend` block with Docker's internal DNS resolver (`127.0.0.11`) and a dynamic `$upstream` variable so container hostnames are re-resolved after rebuilds without restarting the gateway. Simplified `AUDIT_LOG_MODEL_EVENTS` documentation in `env.production.example` — leaving it empty now enables built-in defaults covering all models. Added per-component availability and consequences views in the BIA module with dedicated forms, help text, and updated translations. |
| 2026-03-25 | DPIA assessment detail page restyled | Rewrote `assessment_detail.html` to match the application's Tailwind conventions. Removed the redundant double-wrapping container div (base layout already provides the `max-w-5xl` container). Replaced Bootstrap-style `flex flex-wrap -mx-3 g-3` form/grid patterns with native Tailwind `grid grid-cols-N gap-4`. Updated all four cards (Details, Questions, Risks, Measures) and their inline edit modals to use `bg-[var(--color-surface)]` thead, `px-4 py-3` cell padding, `divide-y divide-[var(--color-border)]` tbody separators, and `[&_tbody_tr]:hover:bg-primary-500/5` row hover (matching BIA/threat modules). Removed duplicate CSS class declarations. Replaced Bootstrap-only classes (`d-inline`, `modal-lg`, `modal-dialog-centered`, `btn-close-white`) with Tailwind equivalents. Restyled the sidebar `<dl>` from a flex-gutter layout to `grid grid-cols-[5fr_7fr]`. Added `font-semibold` to all card header labels. |
| 2026-03-23 | Threat module: Bootstrap → Tailwind migration (2026.3.14) | Removed all Bootstrap utility classes and JS components from all 13 threat module templates. Replaced layout helpers (`d-flex`, `flex-column`, `fw-*`, `fs-*`, `ms-*`, `me-*`, `text-uppercase`, `text-muted`, `small`, `badge bg-*`, `row g-3`/`col-*`, `table table-sm`, `form-control`, `form-select`, `form-check`) with Tailwind equivalents. Replaced Bootstrap CSS variables (`--bs-border-color-translucent`, `--bs-secondary-color`, `--bs-tertiary-bg`, `--bs-body-color`) with app theme variables (`--color-border`, `--color-muted`, `--color-surface-hover`, `--color-text`). Rewrote the Bootstrap Offcanvas panel in `library/entries.html` as a vanilla JS slide-in drawer with a backdrop overlay (removes Bootstrap JS dependency from the threat library page entirely). |
| 2026-03-18 | Dependency security fixes (2026.3.13) | Pinned `pyopenssl>=26.0.0` (fixes CVE buffer overflow via `webauthn`, CVSS 9.2) and `pyasn1>=0.6.3` (fixes uncontrolled recursion on crafted ASN.1 data via `webauthn`, CVSS 8.7) as explicit dependencies to override vulnerable transitive versions. |
| 2026-03-18 | Threat Modeling module (2026.3.12) | Added a new **Threat Modeling** module with full STRIDE-LM support. Introduces `ThreatModel`, `ThreatModelAsset`, and `ThreatScenario` models with risk scoring (likelihood × impact → level), treatment options (accept/mitigate/transfer/avoid), scenario lifecycle statuses, and M2M links to CSA controls and BIA components. Full CRUD routes, archive support, and CSV/HTML/PDF export of scenarios. Two Alembic migrations added; module registered as a default. BIA dashboard export sidebar refactored into collapsible dropdown menus. Translations expanded and tests added. |
| 2026-03-13 | BIA templates and utils cleanup | Polished `all_consequences`, `components`, and `requirements` templates; fixed a minor routing issue in BIA routes and corrected a utility function edge case. |
| 2026-03-10 | Audit trail fixes and translation refresh | Fixed the audit trail page layout and corrected missing/broken translation keys across the application (release 2026.3.10). |
| 2026-03-09 | BIA IT dependencies and consequence management (2026.3.9) | Improved UI for editing BIA components and managing consequences; fixed IT dependency handling and enhanced translations for better user experience. |
| 2026-03-09 | Information classification labels and MFA improvements (2026.3.8) | Overhauled information classification label display; strengthened MFA flow with usability and reliability improvements. |
| 2026-03-08 | Tools section added (2026.3.7) | New **Tools** section with five interactive utilities: AI Act Checker, CVSS Calculator, Risk Tool, Security Roadmap, and Cloud Sovereignty Framework. |
| 2026-03-07 | PDF export and CSS fixes (2026.3.6) | Added PDF export functionality for reports; fixed CSS inconsistencies in HTML/PDF exports. |
| 2026-03-07 | BIA dependencies overview fix (2026.3.5) | Corrected the dependencies overview display. |
| 2026-03-02 | Dashboard, filtering and translation fixes (2026.3.4) | Fixed multiple dashboard rendering issues, corrected filter behaviour, and updated translations. |
| 2026-03-01 | Dependency vulnerability fixes (2026.3.3) | Updated third-party dependencies to resolve known security vulnerabilities. |
| 2026-02-28 | Dashboard and visualisation updates (2026.3.2) | Fixed dashboard layout bugs and refreshed chart/visualisation components. |
| 2026-02-28 | BIA overview revamp and export button fix (2026.3.1) | Fixed broken export buttons on the BIA dashboard and context detail page; revamped the BIA overview page with richer information and a more user-friendly layout. |
| 2026-02-27 | Full Tailwind migration and consequence/maturity fixes (2026.2.12) | Removed Bootstrap entirely in favour of Tailwind CSS; fixed the availability and consequences form; corrected maturity assessment scoring logic; broadened consequence tables; updated translations. |
| 2026-02-25 | BIA archiving (2026.2.11) | Introduced BIA context archiving: new `bia_archive` migration, archive/unarchive routes, a dedicated archived-contexts view, and corresponding tests. Dashboard now shows archived count. |
| 2026-02-24 | Maturity scoring and bug fixes (2026.2.10) | Added scoring logic to the CMMI maturity assessment module; addressed miscellaneous bug fixes across the application. |
| 2026-02-24 | Author full-name fix for coordinator/responsible fields (2026.2.9) | Coordinator and responsible fields on BIA contexts now store the user's full name instead of their username. |
| 2026-02-18 | BIA context and component form overhaul (2026.2.8) | Significantly expanded BIA forms with improved validation and UX; revamped the context form template; updated the components list and edit-component views; refreshed English and Dutch translations. |
| 2026-02-15 | BIA component type/description fields and security hardening (2026.2.7) | Edit-component form now captures component type and description; tightened security headers; various CSS and JS quality improvements. |
| 2026-02-14 | BIA Tier classification | Added `BiaTier` model and management interface. BIA Contexts clearly display their criticality tier (Critical Infrastructure, Mission Critical, etc.), and admins can customize tier names via a new settings panel. |
| 2026-02-08 | BIA environment exports and audit hardening | Component environments appear in CSV/SQL exports, HTML export reuses the context detail template with print styles, audit event JSON parsing is more tolerant, and translations refreshed. |
| 2025-11-28 | UI export parity and layout polish | Navbar collapse toggler now hides on large screens, BIA detail/context cards use a flex row so the Risk/Overview/Summary stack stays adjacent while exports inline `app.css` for identical styling, and CSA dashboard cards/buttons share the same responsive row to avoid wrapping. |
| 2025-11-23 | CSA assessment deletion controls | Admins and assessment managers can now remove assessments (with cascaded responses/assignments) from the detail view, plus new translations and tests. |
| 2025-11-23 | Optional Certbot & security overlays | Added automated Let's Encrypt support plus fail2ban and CrowdSec sidecars for standalone deployments, with new `.env.production` toggles and compose overlays. |
| 2025-11-16 | DPIA workflow overhaul | Added colored status badges (in progress, in review, finished, abandoned), CRUD controls for risks/measures, and severity badge alignment with BIA colors. |
| 2025-11-15 | CSS-only form tooltips and translations | Added reusable tooltip macro, styling, and localisation updates for BIA forms with accessible hover/focus helpers. |
| 2025-11-05 | BIA owner assignment and AI risk display updates | Owner reassignment no longer changes last-update timestamps, risk indicators show yes/no, and AI risk tables now include motivation, colour-coded badges, and translations. |
| 2025-11-04 | CSA Question translatoon, work on some of the badges | |
| 2025-10-31 | Backup service and operational tooling added | Added a dedicated backup container, compression, retention (2 days), optional S3 upload, status endpoint and healthcheck for automated backups. |
| 2025-10-30 | Translated all parts of the application, add docker support. | |
| 2025-10-29 | Localization enhancements | Added per-user locale preference, login selector, and rebuilt translation catalogs. |
| 2025-10-24 | Navigation refactored | Introduced dynamic Bootstrap dark-mode nav with registry-driven entries. |
| 2025-10-24 | Admin module scaffolded | Ported MFA management to shared helpers and added admin blueprint. |
| 2025-10-24 | Auth flow consolidated | Centralised MFA/session helpers and reintroduced security headers. |
| 2025-10-24 | Unified model registry noted | Documented migration/back-compat requirements for merged schema. |
| 2025-10-24 | BIA detail view migrated | Added CIA impact summary, component table, and AI indicators. |
| 2025-10-24 | Alembic environment added | Unified migrations harness prepared for future revisions. |
| 2025-10-24 | Session security & auth scaffolded | Combined auth blueprint, MFA utilities, and middleware. |
| 2025-10-24 | BIA/CSA models scaffolded | Namespaced models registered under unified metadata. |
| 2025-10-24 | Scaffold repository initialised | Created Poetry project, registry skeleton, documentation placeholders. |
| TBD | BIA models migrated | Pending reconciliation of models and migrations. |
| TBD | CSA models migrated | Pending integration work. |
| TBD | Unified auth released | Merge MFA flows, role mapping, and session security. |
| TBD | Bootstrap dark theme complete | Template consolidation and design review. |
| TBD | Database guides published | Hands-on docs for PostgreSQL deployments. |
